# PRD 结构化分析报告

**PRD 文件:** sample-prd.md
**分析时间:** 2026-07-09T10:00:00Z
**分析版本:** v1.0
**分析模式:** 规则元组 + 状态迁移表 + 隐性规则显性化

---

## 1. 规则元组

### 1.1 计算规则

| 规则ID | 规则描述 | 类型 | 公式/约束 | 优先级 |
|--------|---------|------|---------|--------|
| R001 | 实付金额 = 商品总额 - 优惠券抵扣 + 运费 | 计算公式 | `payable = max(0, total - coupon + shipping)` | P0 |
| R002 | 运费 = f(城市等级) | 函数映射 | 一线10/二线15/其他20 | P0 |
| R003 | 实付金额 >= 0 | 边界约束 | `Math.max(0, result)` | P0 |
| R004 | 满99免运费 | 条件规则 | `total >= 99 → shipping = 0` | P1 |
| R005 | 优惠券不超过总额 | 约束条件 | `coupon <= total` | P1 |
| R006 | 一单只能用一张优惠券 | 约束条件 | `coupons.length <= 1` | P1 |

### 1.2 降级规则

| 规则ID | 规则描述 | 触发条件 | 降级策略 | 优先级 |
|--------|---------|---------|---------|--------|
| R007 | 地址为空 → 默认运费 | `address == null \|\| address.city == ''` | `shipping = 15` | P0 |
| R008 | 优惠券服务不可用 → 无优惠 | 服务调用超时/失败 | `coupon = 0` | P1 |
| R009 | 运费服务不可用 → 默认运费 | 服务调用超时/失败 | `shipping = 15` | P1 |
| R010 | 支付超时 → 重试 | 支付系统响应超时 | 重试3次，间隔2秒 | P0 |

## 2. 状态迁移表

### 2.1 合法迁移

| 当前状态 | 触发动作 | 目标状态 | 条件 | 触发方 |
|---------|---------|---------|------|--------|
| — | 创建订单 | PENDING | 无 | 用户 |
| PENDING | 支付成功 | PAID | 金额匹配 | 支付回调 |
| PENDING | 用户取消 | CANCELLED | 无 | 用户 |
| PENDING | 超时30分钟 | CANCELLED | 自动 | 定时任务 |
| PAID | 商家发货 | SHIPPED | 无 | 商家 |
| PAID | 退款审批通过 | CANCELLED | 管理员审批 | 管理员 |
| SHIPPED | 用户确认 | COMPLETED | 无 | 用户 |
| SHIPPED | 7天自动确认 | COMPLETED | 自动 | 定时任务 |

### 2.2 非法迁移

| 当前状态 | 尝试动作 | 预期结果 | 错误信息 |
|---------|---------|---------|---------|
| CANCELLED | 任何变更 | 拒绝 | "已取消的订单不能变更状态" |
| COMPLETED | 任何变更 | 拒绝 | "已完成的订单不能变更状态" |
| SHIPPED | 取消 | 拒绝 | "已发货订单不能直接取消" |
| PAID | 重复支付 | 幂等返回 | "订单已支付" |

### 2.3 并发竞态

| 竞态场景 | 触发条件 | 预期行为 | 实现要求 |
|---------|---------|---------|---------|
| 支付+超时竞态 | T+30min 同时收到支付回调和超时触发 | 只有一个生效 | 分布式锁/乐观锁 |
| 重复支付回调 | 支付系统多次回调 | 幂等处理 | 状态检查+唯一键 |
| 并发取消 | 用户和定时任务同时取消 | 幂等处理 | CAS/唯一键 |

## 3. 边界场景矩阵

| 场景ID | 场景描述 | 边界值 | 预期行为 | 风险等级 |
|--------|---------|--------|---------|---------|
| B001 | 优惠券恰好等于总额 | coupon = total | 实付 = 运费 | 低 |
| B002 | 优惠券超过总额 | coupon > total | 优惠按总额算，实付 = 运费 | 中 |
| B003 | 订单金额恰好99元 | total = 99 | 免运费 | 低 |
| B004 | 订单金额98.99元 | total = 98.99 | 不免运费 | 中 |
| B005 | 0元订单 | total = 0, coupon = 0 | 实付 = 运费 | 低 |
| B006 | 超时边界 T+29:59 | 超时前1秒 | 仍可支付 | 高 |
| B007 | 超时边界 T+30:01 | 超时后1秒 | 已取消 | 高 |
| B008 | 自动确认边界 T+7d | 恰好7天 | 自动确认 | 中 |
| B009 | 分页边界 | 第 N 页最后一条 | 不重复不遗漏 | 中 |
| B010 | 6个月边界 | 恰好6个月前 | 展示/不展示（需明确） | 中 |

## 4. 异常处理逻辑

| 异常ID | 异常类型 | 触发条件 | 预期处理 | 日志级别 | 验证方式 |
|--------|---------|---------|---------|---------|---------|
| E001 | 支付超时 | 支付系统响应 > 5s | 重试3次，间隔2s | WARN | 故障注入测试 |
| E002 | 回调数据异常 | 回调金额 != 订单金额 | 记录日志，不变更状态 | ERROR | 单元测试 |
| E003 | 优惠券服务不可用 | 调用超时/返回错误 | 降级为 coupon=0 | WARN | 单元测试 |
| E004 | 运费服务不可用 | 调用超时/返回错误 | 降级为 shipping=15 | WARN | 单元测试 |
| E005 | 重复回调 | 同一支付流水号多次到达 | 幂等返回成功 | INFO | 并发测试 |
| E006 | 过期优惠券 | 使用时间 > 有效期 | 拒绝使用，提示过期 | INFO | 单元测试 |

## 5. 隐性规则显性化

| 规则ID | 隐性规则 | 来源（显性描述） | 推导逻辑 | 风险等级 | 验证方式 |
|--------|---------|----------------|---------|---------|---------|
| QI001 | 金额必须用分(整数)计算 | "金额计算" | 浮点精度问题 → 必须用分 | 🔴 高 | 代码审查 |
| QI002 | create_time 必须有索引 | "按创建时间倒序" | ORDER BY 无索引 → 全表扫描 | 🔴 高 | EXPLAIN |
| QI003 | 列表查询必须分页 | "订单列表" | 数据量无上限 → 内存溢出 | 🔴 高 | 代码审查 |
| QI004 | 分页上限50条 | "每页最多50条" | LIMIT 硬编码 | 低 | 代码审查 |
| QI005 | 数据权限隔离 | "用户查看自己的订单" | WHERE user_id = current_user | 🔴 高 | 权限测试 |
| QI006 | 支付幂等键 | "重复支付" | 唯一约束防重复扣款 | 🔴 高 | 并发测试 |
| QI007 | 状态变更需乐观锁 | "并发竞态" | version/CAS 防并发 | 🔴 高 | 并发测试 |

## 6. 待确认问题

| 问题ID | 问题描述 | 影响范围 | 建议假设 | 紧急度 |
|--------|---------|---------|---------|--------|
| P001 | 6个月边界：恰好6个月前的订单是否展示？ | 列表查询 | 建议展示（含当天） | 中 |
| P002 | 优惠券超过总额时，是按总额抵扣还是按面额抵扣？ | 金额计算 | 建议按总额抵扣（不找零） | 中 |
| P003 | 退款后库存是否回滚？ | 退款流程 | PRD未提及，建议确认 | 高 |
| P004 | 运费计算的"城市等级"数据源是什么？ | 运费计算 | 需确认是否有城市等级配置表 | 中 |

---

**分析统计:**
- 提取规则总数: 10 (计算) + 10 (状态迁移) + 10 (边界) + 6 (异常) + 7 (隐性) = 43
- 待确认问题: 4
- 高风险项: 5 (QI001, QI002, QI003, QI005, QI006, QI007)
